# 05: 存檔前連線驗證、`--force` 與 `--password-stdin`

**What to build:** 讓使用者在建立連線設定的當場就發現帳密打錯，而不是等到半夜排程失敗；同時讓部署工程師能把建立連線設定這一步也納入自動化，而不必退回使用不安全的命令列參數。

**Blocked by:** 04

**Status:** resolved

- [x] `profile set` 預設會實際連線驗證帳號密碼，重用既有的連線檢查機制
- [x] 驗證失敗時拒絕儲存，並以非零代碼結束
- [x] 提供必須明確指定的強制參數以略過驗證，使用時輸出明顯警告說明該設定尚未被證實可用
- [x] 支援從標準輸入讀取密碼，供自動化部署使用
- [x] 以標準輸入提供密碼時，密碼不出現在任何輸出中
- [x] 密碼在任何情況下都不會出現在命令列參數上

## Comments

> *This was generated by AI during triage.*

已完成。關於「重用既有的連線檢查機制」：直接呼叫 `CheckCompatibilityAsync` 在語意上是錯的——它在客戶端工具未就緒時**根本不連線**就回傳失敗，會把「沒裝 pg_dump」誤判成「帳密錯誤」。因此把既有那段 Npgsql 連線邏輯**原封不動抽出**為 `IToolDetectionService.VerifyConnectionAsync`，`CheckCompatibilityAsync` 改為建構其上——codebase 仍只有一條實際連線路徑，未複製邏輯。

另新增 `Redact()`，避免驅動程式例外訊息回吐密碼。此舉僅涵蓋新的 `profile set` 路徑；spec 列為 Out of Scope 的「GUI 測試連線回吐連線字串」仍待另立票。

修改了票 04 的 `ProfileSet_DoesNotExposeAnyPasswordOption`：原斷言「選項名稱不含 password」會誤判合法的 `--password-stdin`，改為斷言無 `--password`/`-p`，且任何名稱含 password 的選項其 `ValueType` 必須為 `bool`。斷言力道未減弱。
