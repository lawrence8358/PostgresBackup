# 03: CLI 安全行為修正

**What to build:** 讓排程腳本能夠偵測到設定錯誤，並讓僅閱讀說明而未讀文件的使用者也能得知 `-p` 的暴露風險。

**Blocked by:** 無（可立即開始）

**Status:** resolved

- [x] 備份作業指定不存在的連線設定名稱時，以非零代碼結束並輸出明確錯誤（目前僅印警告後繼續執行）
- [x] 還原作業指定不存在的連線設定名稱時，以非零代碼結束並輸出明確錯誤（目前完全靜默）
- [x] 使用 `-p` 參數時輸出一行執行時警告，說明密碼會暴露於行程資訊中，建議改用連線設定
- [x] `-p` 參數本身予以保留，不移除
- [x] `--connection-string` 的說明文字標註僅供互動式除錯，不得用於排程腳本

## Comments

> *This was generated by AI during triage.*

已完成。`backup` / `restore` 找不到連線設定時改為 `[ERROR]` + 退出碼 1，測試同時斷言備份／還原服務從未被呼叫（不只看退出碼）。`-p` 暴露警告加在 `backup`、`restore`、`check-tools` 三處。`-p` 與 `--connection-string` 皆保留。
