# 02: 憑證寫入失敗改為明確失敗

**What to build:** 讓使用者在密碼無法安全儲存時立刻知道，而不是收到「儲存成功」卻在關閉程式後才發現密碼消失，或在排程執行時才靜默失敗。

**Blocked by:** 無（可立即開始）

**Status:** resolved

- [x] 移除憑證寫入失敗時靜默退回行程記憶體明文字典的行為
- [x] 在 Windows 上寫入失敗即拋出例外，不再回報成功
- [x] 圖形介面在儲存連線設定失敗時顯示明確錯誤訊息，不再顯示成功
- [x] 記憶體儲存路徑僅保留給非 Windows 平台，以及由明確旗標開啟的測試情境
- [x] 補上真實 Windows 認證管理員寫入路徑的測試覆蓋（此路徑目前因既有測試使用強制記憶體儲存旗標而完全未被覆蓋）
- [x] 既有的「持久化檔案不含密碼」不變條件測試仍然通過

## Comments

> *This was generated by AI during triage.*

已完成。新增 `CredentialStorageException`（帶 Win32 錯誤碼）；`WindowsCredentialStorage.SetPassword` 不再靜默退回記憶體；WPF `SaveProfileAsync` 失敗時明確報錯；字串已資源化（Core 與 WPF 的 resx 英／繁中皆補齊）。

簽入的測試以「密碼長度超過認證管理員 blob 上限（2560 bytes）」確定性重現寫入失敗，因此「拋例外」與「不保留明文」兩項行為是真正紅燈驗證過的。需要 SYSTEM 身分或人為破壞機器狀態的失敗情境留給票 10。

**已知殘留項目（審查發現，決定另立票）：** `JsonConnectionProfileRepository.SaveProfileAsync` 先寫 JSON metadata、後寫密碼；密碼寫入拋例外時會留下一筆無密碼的設定。介面已會明確報錯，故無「誤以為成功」問題，但半完成狀態需調整寫入順序或加回滾才能清除。
