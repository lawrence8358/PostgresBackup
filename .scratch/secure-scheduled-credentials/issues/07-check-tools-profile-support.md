# 07: `check-tools` 支援 `--profile`

**What to build:** 讓系統管理員檢查客戶端工具版本相容性時不必打出明文密碼，消除目前唯一只能以不安全方式呼叫的指令。

**Blocked by:** 04

**Status:** resolved

- [x] `check-tools` 新增 `--profile` 參數，行為與備份作業、還原作業一致
- [x] 指定不存在的連線設定名稱時以非零代碼結束
- [x] `--connection-string` 予以保留，不移除

## Comments

> *This was generated by AI during triage.*

已完成。找不到連線設定時在**工具偵測之前**即中止，測試以 `Times.Never` 釘住 `DetectAsync` 未被呼叫。`shouldCheckServer` 補上 profile 條件，指定設定時會觸發伺服器版本檢查。至此 `check-tools` 不再是唯一只能以明文密碼呼叫的指令。
