# 08: 圖形介面連線設定畫面說明文字

**What to build:** 避免使用者在圖形介面建好連線設定後，因排程找不到而困惑於「我明明建好了」。

**Blocked by:** 無（可立即開始）

**Status:** resolved

- [x] 連線設定畫面新增一行說明文字，告知此處設定僅供本程式使用，排程與命令列請另以 `profile set` 建立
- [x] 說明文字指向使用手冊的相關章節
- [x] 圖形介面不顯示命令列連線設定清單，不讀取命令列存放區
- [x] 圖形介面的連線設定儲存機制本身不做任何改變

## Comments

> *This was generated by AI during triage.*

已完成。新增資源 key `Settings_Connection_ScopeHint`（`Strings.resx` 英文與 `Strings.zh-TW.resx` 繁中皆有），在 SettingsView 的連線設定卡片內以 11px 斜體次要色呈現。`App.xaml.cs` 的 DI 未動，GUI 不讀命令列存放區。另加一測試釘住兩種語系的資源 key 都存在。
