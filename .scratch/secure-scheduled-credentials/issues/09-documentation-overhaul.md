# 09: 文件全面改寫

**What to build:** 讓文件與實際行為一致，讓讀者能安全地設定無人值守排程，並讓資安稽核人員能一次讀懂密碼存放在哪、誰讀得到、不防護誰。

**Blocked by:** 05, 06, 07

**Status:** resolved

- [x] 改寫使用手冊的排程腳本範例，移除所有密碼相關參數，改用 `--profile`
- [x] 新增以 SYSTEM 身分註冊排程任務的操作說明
- [x] 說明為何不建議以個人帳號執行排程（Windows 密碼到期會導致任務靜默停止）
- [x] 新增 `profile` 指令群組的完整參數說明與使用範例，含以標準輸入提供密碼的自動化用法
- [x] 修正 README 的「無明文憑證」宣稱
- [x] 修正使用手冊中將 `-p` 描述為「安全環境變數傳遞」的錯誤敘述
- [x] 修正常見問題中關於命令列串接的不精確說明
- [x] 移除 README 中「介面建立的連線設定可立即透過命令列使用」的承諾
- [x] 使用手冊新增獨立的「安全性說明」一節，涵蓋兩套連線設定各自的存放位置、可讀取者、加密的實際保護範圍，以及明確不防護的對象
- [x] 安全性說明一節以一般讀者能理解的語言撰寫，避免堆砌術語
- [x] README 僅放一段摘要並連結至該節
- [x] 不新增 `SECURITY.md`

## Comments

> *This was generated by AI during triage.*

已完成，涉及 `README.md`（英）、`README.zh-TW.md`（繁）、`docs/USER_MANUAL.md`（新增 §4.4 與 §6，FAQ 順延為 §7）。

自我檢查：`YourPassword`、「安全環境變數傳遞」、「零明文」在三份文件中皆已清零；`SECURITY.md` 未建立。（`dist/` 下仍有舊副本，但該目錄已被 gitignore，由 `build.ps1` 從來源文件重新產生，非問題。）

**審查後追加修正：** `--password-stdin` 的範例原本寫成 `"MyS3cretPassword" | pgbackup profile set ...`，而同段文字宣稱「密碼不會出現在指令歷史或處理序命令列上」——該範例本身就把明文密碼送進 PowerShell 歷史紀錄，與宣稱矛盾。三份文件已改為自權限受控檔案或注入的環境變數讀取，並明確警告管線左側同樣要緊。

**待辦（審查發現，尚未處理）：** 兩份 README 的資料儲存位置表把備份歷史標為 "Both"／「兩者」，但 `SqliteBackupHistoryRepository` 使用 `SpecialFolder.LocalApplicationData`——以 SYSTEM 身分執行的排程備份會寫入 `C:\Windows\System32\config\systemprofile\AppData\Local\PostgresBackup\`，**不會**出現在圖形介面的歷史頁。這是本批變更推薦 SYSTEM 排程後帶來的新使用者意外，需修正表格並在手冊 §5 補充說明。
