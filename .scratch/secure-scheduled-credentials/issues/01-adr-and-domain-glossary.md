# 01: 架構決策記錄與領域詞彙更新

**What to build:** 讓未來的維護者能理解「命令列為何不沿用 Windows 認證管理員」這個看似反直覺的決定，並讓團隊在討論時能明確區分兩種連線設定。

**Blocked by:** 無（可立即開始）

**Status:** resolved

- [x] 建立 `docs/adr/` 並寫入第一份架構決策記錄，記載「命令列採用機器範圍加密存放區、與介面連線設定完全獨立」的決定
- [x] 記錄中包含已評估但未採用的方案：以 SYSTEM 身分寫入 Windows 認證管理員、改用 Windows 整合驗證免除密碼，以及各自未採用的理由
- [x] 記錄中載明驅動此決定的三項環境限制：無法建立專用服務帳號、個人管理員帳號密碼每三個月強制更換、無法變更 PostgreSQL 伺服器設定
- [x] 記錄中明確載明已接受的風險：具本機系統管理員權限者可解開加密存放區
- [x] `CONTEXT.md` 將現有含糊的「連線設定」分化為「介面連線設定」與「命令列連線設定」兩個詞條，各自標註應避免的同義詞
- [x] `CONTEXT.md` 新增「機器範圍加密」詞條
- [x] `CONTEXT.md` 維持純詞彙表性質，不含任何實作細節

## Comments

> *This was generated by AI during triage.*

已完成。ADR 為 `docs/adr/0001-cli-machine-scoped-credential-store.md`；`CONTEXT.md` 的「連線設定」已分化為「介面連線設定」「命令列連線設定」，並新增「機器範圍加密」詞條。純文件變更，未觸碰程式碼。
