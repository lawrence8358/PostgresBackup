# 06: `profile list` 與 `profile remove`

**What to build:** 讓系統管理員能隨時確認排程會用到的連線設定是否存在、密碼是否正常、存放區權限是否仍然安全，並能清除不再需要的設定。

**Blocked by:** 04

**Status:** resolved

- [x] `profile list` 顯示名稱、主機與連接埠、資料庫、使用者名稱、來源標示與建立時間
- [x] `profile list` 顯示密碼狀態（已設定／遺失），僅呈現有無，不呈現內容或長度
- [x] `profile list` 的輸出在任何情況下都不包含密碼字串
- [x] `profile list` 檢查存放區檔案權限是否仍符合預期，不符則輸出警告
- [x] 權限檢查僅在 `profile set` 與 `profile list` 執行，備份作業與還原作業執行時不檢查
- [x] `profile remove` 移除設定項目並一併清除其加密密碼
- [x] 刪除後 `profile list` 不再顯示該項目

## Comments

> *This was generated by AI during triage.*

已完成。權限判斷條件：繼承已停用、且允許身分全部落在 Administrators（`S-1-5-32-544`）與 SYSTEM（`S-1-5-18`）兩個 SID 內。目錄不存在或探測回傳 null 時**不下判斷、不警告**，避免存放區尚未建立時誤報。測試涵蓋繼承未關閉與多一個 Everyone（`S-1-1-0`）兩種不安全情形。權限檢查亦依票面要求補進 `profile set`。

**後續變更：** `profile set` 的權限處理已在安全修正中改為「檢查在寫入之前、不符即拒絕儲存」，不再只是警告；`profile list` 維持警告不變。位置改由相依注入提供（`MachineScopedStoreLocation`），解決「檢查的目錄」與「寫入的目錄」脫鉤的問題。詳見票 11。
