# PostgresBackup

## Agent skills

### 議題追蹤（Issue tracker）

議題與規格文件以 markdown 檔案形式存放於 `.scratch/<feature-slug>/`，隨版控一併簽入。本專案未使用 GitHub Issues。

詳見 `docs/agents/issue-tracker.md`。

### 分流標籤（Triage labels）

採用五個標準分流角色，標籤字串維持預設值：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。

詳見 `docs/agents/triage-labels.md`。

### 領域文件（Domain docs）

單一 context 配置：詞彙表為根目錄的 `CONTEXT.md`，架構決策記錄（ADR）置於 `docs/adr/`。

詳見 `docs/agents/domain.md`。
