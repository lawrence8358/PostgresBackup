<UserControl x:Class="PostgresBackup.Wpf.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:l="clr-namespace:PostgresBackup.Wpf.Extensions"
             mc:Ignorable="d">
    <Grid Margin="24,20,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 頁面標題 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
            <Border Width="4" Height="24" Background="{DynamicResource PrimaryBrush}" CornerRadius="2" Margin="0,0,12,0"/>
            <TextBlock Text="{l:Loc Log_Title}" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- ── 卡片: 全域主控台日誌 ── -->
        <hc:Card Grid.Row="1" Padding="0" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
            <DockPanel>
                <!-- 標頭工具列 -->
                <Border DockPanel.Dock="Top" Background="{DynamicResource PrimaryBrush}" CornerRadius="4,4,0,0" Padding="18,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Path Data="{StaticResource IconLogGeometry}" Fill="White"
                                  Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{l:Loc Log_StreamTitle}" FontSize="14" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="{l:Loc Backup_ClearLog}"
                                    Style="{StaticResource ButtonDefault}"
                                    Command="{Binding ClearLogCommand}"
                                    Padding="10,2" Margin="0,0,8,0" Height="26" FontSize="11"/>
                            <Button Content="{l:Loc Backup_CopyLog}"
                                    Style="{StaticResource ButtonDefault}"
                                    Command="{Binding CopyLogCommand}"
                                    Padding="10,2" Height="26" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 終端文字區塊 -->
                <Border Background="#181818" CornerRadius="0,0,4,4" Padding="14">
                    <TextBox x:Name="LogTextBox"
                             Style="{StaticResource TerminalTextBoxStyle}"
                             Text="{Binding LogContent, Mode=OneWay}"
                             TextChanged="LogTextBox_TextChanged"/>
                </Border>
            </DockPanel>
        </hc:Card>
    </Grid>
</UserControl>
